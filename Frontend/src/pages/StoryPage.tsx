import React from 'react';

const StoryPage: React.FC = () => (
  <div className="page-container">
    <h1 className="page-title playfair">Our Story</h1>
    <p style={{fontSize: '1.2rem', color: '#555', maxWidth: '800px', margin: '0 auto'}}>
      Our journey began with a simple mission — to bring the purest and finest quality dry fruits to every home.
    </p>
  </div>
);

export default StoryPage;
